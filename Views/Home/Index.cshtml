@model HomeViewModel
<h1>Лучшие автомобили</h1>
<div class="row mt-5 mb-2">
    @{
        foreach (Car car in Model.favouriteCars)
        {
            @Html.Partial("_AllCars", car)
        }
    }
</div>

